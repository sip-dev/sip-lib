{
  "name": "sip-lib",
  "version": "0.1.0",
  "description": "lib",
  "author": "sip",
  "homepage": "https://github.com/sip-dev/sip-lib/",
  "bugs": {
    "url": "https://github.com/sip-dev/sip-lib/issues"
  },
  "engines": {
    "node": ">= 4.2.1",
    "npm": ">= 3"
  },
  "license": "MIT",
  "scripts": {
    "test": "ng test -sr -sm=false",
    "build": "rimraf dist && tsc && copy package.npm.json dist\\\\package.json /y",
    "npmpublish": "npm run build && npm pu ./dist"
  },
  "dependencies": {},
  "devDependencies": {
    "@types/node": "~8.9.4",
    "rimraf": "^2.6.2",
    "rxjs": "^6.0.0",
    "typescript": "~2.7.2"
  }
}
